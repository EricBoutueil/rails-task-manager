<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <p><%= check_box_tag "task[compeleted]", task.id, task.completed %> <%= link_to task.title, task_path(task) %></p>
<% end %>

<br>
<h2><%= link_to "🆕 Add a new task", tasks_new_path %></h2>

