<h1>My tasks</h1>

<% @tasks.each do |task| %>
  <p><%= check_box_tag "task[compeleted]", task.id, task.completed %> <%= link_to task.title, task_path(task) %> | <%= link_to "Delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %></p>
<% end %>

<br>
<h2><%= link_to "🆕 Add a new task", tasks_new_path %></h2>

